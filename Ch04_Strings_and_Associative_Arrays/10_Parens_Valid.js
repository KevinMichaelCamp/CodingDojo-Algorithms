/* Create a function that, given an input string str, returns a boolean whether parenthesis in str are valid.  Valid sets of parentheses always open before they close.  For example, for "Y(3(p)p(3)r)s" return true.  Given "N(0(p)3" retun false; not every parentheses is closed.  Given "N(0)t )0(k", return false, because the second ) is premature; there is nothing open for it to close*/
function paransValid(str){
    
}